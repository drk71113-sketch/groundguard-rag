"""Built-in, replaceable adapters for GroundGuard-RAG ports.

Adapters live outside the domain and application layers so the stable
contracts never depend on a particular implementation.  Built-ins remain
explicit and replaceable; none constructs a hidden provider client.
"""
