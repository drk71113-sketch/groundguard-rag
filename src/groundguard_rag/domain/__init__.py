"""Domain layer: enums, data models, config, exceptions, and ports.

Nothing in this package may import a concrete RAG framework, vector store,
LLM SDK, or MCP type -- those dependencies belong strictly in adapter layers
outside ``groundguard_rag.domain``.
"""
