# Local evaluation data

Place private labels, intermediate annotations, and local evaluation inputs in
this directory. Their contents are ignored by Git by default. The small,
reviewed public outputs shipped with the repository live under
`demo_rag/results/samples/`.

Never place API keys, private prompts, customer documents, or model credentials
in public evaluation artifacts.
